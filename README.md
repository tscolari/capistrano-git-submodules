CapistranoGitSubmodules
---------------------

Replaces the default git task from capistrano to use
git-submodule for creating releases

Installation
------------

```ruby
  # Gemfile
  gem 'capistrano-git-submodules'
```

```ruby
  # Capfile
  require 'capistrano/git_submodules'
```

deploy!
